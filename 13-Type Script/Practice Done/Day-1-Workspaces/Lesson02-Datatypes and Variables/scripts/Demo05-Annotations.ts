// Example of Function Annotations 
function display(id: number, name: string) {
    console.log("Id : " + id + ", Name = " + name);
}

// Example of Type Annotations Object
var employee : {
    id: number;
    name: string;
};

// In Traditional JS Directly Initailizing Where As Here We First Create 
// the Type and Later Initialize 
employee = {
    id: 200,
    name: "Steven"
};

